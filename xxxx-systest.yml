- hosts:
  - localhost
  vars:
    joey:
      - a
      - b
  tasks:
    - debug: msg="Hello world?"
    - name: Write out host-names
      lineinfile: line="{{item}}" dest={{lookup("env","PWD")}}/xyz state=present  create=yes insertafter=EOF
      with_items: joey
    - fail: "really. fail."


- hosts:
  -  launched
  gather_facts: no
  vars:
     ansible_ssh_user: ec2-user
     ansible_ssh_private_key_file: /Users/jab/.ssh/ec2-keypair.pem
  roles:
    - centos-systest

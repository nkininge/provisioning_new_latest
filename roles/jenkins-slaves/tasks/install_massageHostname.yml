- name: install_massageHostname | Install EC2 hostname
  sudo: yes
  sudo_user: root
  cron: name="Install standard EC2 hostname"
        job="hostname `  curl -s http://169.254.169.254/latest/meta-data/hostname  ` "
        special_time=reboot user=root
  tags: install_massageHostname
